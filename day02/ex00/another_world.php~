#!/usr/bin/php
<?php
    $arr = array();
    $arr = array_merge($arr, preg_split("/[' ',]+/", trim($argv[1])));
    $j = 0;
    $count = count($arr);
    $string = trim(implode(' ',$arr));
    echo $string . "\n";